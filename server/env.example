# Server Configuration
# Environment Configuration
# Set to: development, staging, or production
# Priority: ENVIRONMENT > NODE_ENV
ENVIRONMENT=development
NODE_ENV=development
PORT=5000

# MongoDB Configuration
#MONGODB_URI=mongodb://localhost:27017/admin_panel
#MONGODB_DB_NAME=admin_panel

MONGODB_URI=mongodb://yard_user:lkgfukgfgfkjhtrdtdkgsjfdsjbdgvdhkgkklffFHFHdS@13.235.246.242:57019/yard_db_app

MONGODB_DB_NAME=yard_db_app


# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d

# CORS Configuration
# Development: Allow local frontend
# Production: Allow production frontend domain (comma-separated)
CORS_ORIGIN=http://localhost:5173,http://localhost:5174,http://localhost:3000

# API Configuration
API_PREFIX=/api/v1

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Redis Configuration (Optional - for caching, sessions, queues)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# File Upload Configuration
MAX_FILE_SIZE=5242880
UPLOAD_PATH=./uploads
ALLOWED_FILE_TYPES=jpg,jpeg,png,gif,pdf,doc,docx

# Cloudinary Configuration (Optional - for image hosting)
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# AWS S3 Configuration (Optional - for file storage)
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
AWS_REGION=us-east-1
AWS_S3_BUCKET_NAME=your-bucket-name

# Bull Queue Configuration
BULL_REDIS_HOST=localhost
BULL_REDIS_PORT=6379
BULL_REDIS_PASSWORD=
BULL_REDIS_DB=1

# Swagger/API Documentation
# Development: true (enabled)
# Production: false (disabled for security)
SWAGGER_ENABLED=true
SWAGGER_PATH=/api-docs

# Logging
LOG_LEVEL=info
LOG_FILE=./logs/app.log

# Monitoring (Prometheus)
METRICS_ENABLED=true
METRICS_PATH=/metrics

# Google OAuth Configuration
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# API Basic Authentication (Required for all public endpoints)
# Generate random keys: node -e "const crypto = require('crypto'); console.log('API_KEY=' + crypto.randomBytes(32).toString('hex')); console.log('API_SECRET=' + crypto.randomBytes(32).toString('hex'));"
# Mobile apps must include these credentials in HTTP Basic Auth header:
# Authorization: Basic base64(API_KEY:API_SECRET)
API_KEY=your-api-key-here-generate-random-64-char-hex-string
API_SECRET=your-api-secret-here-generate-random-64-char-hex-string

# Swagger Documentation Basic Authentication (Simple password protection)
# When accessing /api-docs, browser will prompt for username and password
SWAGGER_USERNAME=admin
SWAGGER_PASSWORD=I9dUyVAWjVVY6Cj4
